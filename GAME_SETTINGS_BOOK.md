# GAME SETTINGS BOOK

## Tone Pillars

- 세계관은 `터미널 판타지 + 시스템 복구 작전`이다.
- 문장은 가볍게 농담치기보다 `짧고 명확한 작전 브리핑`에 가깝게 쓴다.
- 플레이어를 띄우기보다 `위험, 준비, 보급, 복구`를 중심으로 긴장을 유지한다.
- 과도한 기술 농담보다 `현장감 있는 은유`를 우선한다.

## Text Layers

- `NPC 직접 대사`
  상점 인사, 현장 지시, 짧은 감정 표현에 사용한다.
  말투는 사람마다 다르되 세계관 용어는 통일한다.
- `브리핑 메모`
  퀘스트 훅, 게시판 요약, 다음 목표 안내에 사용한다.
  플레이어를 움직이게 하는 실무형 문장으로 유지한다.
- `장면 설명`
  지역 분위기, 전황 변화, 결과 정리에 사용한다.
  서술형으로 쓰되 설명문 티를 줄이고 현장 감각을 우선한다.

## Term Rules

- 제도권/작전 조직은 플레이어를 `오퍼레이터`로 부른다.
- 상인/민간인은 `손님`, `오퍼레이터`, 혹은 호칭 생략으로 부른다.
- `플레이어`, `유저`, `코드 워커` 같은 바깥 표현은 UI 내부 문장 외에는 쓰지 않는다.
- `퀘스트`는 시스템 용어로 허용하되, 서사 문장 안에서는 `의뢰`, `작전`, `브리핑`을 우선한다.

## Frontend Copy Rules

- 브라우저에 직접 노출되는 핵심 설명은 한국어 우선으로 제공한다.
- 영어 병기는 고유명사 보조 표기일 때만 허용한다.
- 상점/인벤토리/전투 카드에서는 긴 설정문보다 `지금 왜 필요한가`를 먼저 보여 준다.

## Quest Presentation Rules

- `hook`는 `브리핑 메모`다.
  플레이어가 지금 무엇을 해야 하는지 바로 알 수 있어야 한다.
- `npcLine`은 `NPC 직접 대사`다.
  한두 문장 안에서 사람의 말투와 감정을 보여 주되 목표 설명은 `hook`와 경쟁하지 않게 짧게 쓴다.
- `storyBeat`는 `장면 설명`이다.
  전선이 어떻게 바뀌는지, 이번 임무가 어떤 문맥에 있는지 설명한다.
- `featuredNpc`가 있으면 UI에서 브리핑 출처를 함께 보여 준다.
